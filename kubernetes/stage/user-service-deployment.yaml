apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: ecommerce-stage
  labels:
    app: user-service
    environment: staging
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
        environment: staging
    spec:
      containers:
        - name: user-service
          image: selimhorri/user-service-ecommerce-boot:stage-latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8700
          envFrom:
            - configMapRef:
                name: microservices-config
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8700
            initialDelaySeconds: 20
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8700
            initialDelaySeconds: 40
            periodSeconds: 30
          resources:
            requests:
              cpu: 150m
              memory: 256Mi
            limits:
              cpu: 300m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: ecommerce-stage
  labels:
    app: user-service
    environment: staging
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - name: http
      port: 8700
      targetPort: 8700
