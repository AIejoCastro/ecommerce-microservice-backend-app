apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: ecommerce-stage
  labels:
    app: product-service
    environment: staging
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
        environment: staging
    spec:
      containers:
        - name: product-service
          image: alejocastro/product-service-ecommerce-boot:stage-latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8500
          envFrom:
            - configMapRef:
                name: microservices-config
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8500
            initialDelaySeconds: 20
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8500
            initialDelaySeconds: 40
            periodSeconds: 30
          resources:
            requests:
              cpu: 150m
              memory: 256Mi
            limits:
              cpu: 300m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: ecommerce-stage
  labels:
    app: product-service
    environment: staging
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
    - name: http
      port: 8500
      targetPort: 8500
